name: Deploy Directus to production

on:
  pull_request:
    branches:
      - production
    types: [closed]
    paths:
      - "directus/npm-requirements.txt"

jobs:
  Deploy:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged
    concurrency: directus-production
    container:
      image: node:16
    steps:
      - name: Deploying code
        run: echo "Deploying Directus to production"
